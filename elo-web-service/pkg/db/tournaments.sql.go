// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tournaments.sql

package db

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const addMatchTournament = `-- name: AddMatchTournament :exec
INSERT INTO match_tournament (match_id, tournament_id)
VALUES ($1, $2)
ON CONFLICT DO NOTHING
`

type AddMatchTournamentParams struct {
	MatchID      int32 `json:"match_id"`
	TournamentID int32 `json:"tournament_id"`
}

func (q *Queries) AddMatchTournament(ctx context.Context, arg AddMatchTournamentParams) error {
	_, err := q.db.Exec(ctx, addMatchTournament, arg.MatchID, arg.TournamentID)
	return err
}

const addTournamentMember = `-- name: AddTournamentMember :exec
INSERT INTO tournament_player_membership (tournament_id, player_id)
VALUES ($1, $2)
ON CONFLICT DO NOTHING
`

type AddTournamentMemberParams struct {
	TournamentID int32 `json:"tournament_id"`
	PlayerID     int32 `json:"player_id"`
}

func (q *Queries) AddTournamentMember(ctx context.Context, arg AddTournamentMemberParams) error {
	_, err := q.db.Exec(ctx, addTournamentMember, arg.TournamentID, arg.PlayerID)
	return err
}

const countTournamentMembers = `-- name: CountTournamentMembers :one
SELECT COUNT(*)::int AS member_count
FROM tournament_player_membership
WHERE tournament_id = $1
`

func (q *Queries) CountTournamentMembers(ctx context.Context, tournamentID int32) (int32, error) {
	row := q.db.QueryRow(ctx, countTournamentMembers, tournamentID)
	var member_count int32
	err := row.Scan(&member_count)
	return member_count, err
}

const createTournament = `-- name: CreateTournament :one
INSERT INTO tournaments (name, start_date, end_date)
VALUES ($1, $2, $3)
RETURNING id, name, start_date, end_date
`

type CreateTournamentParams struct {
	Name      string             `json:"name"`
	StartDate pgtype.Timestamptz `json:"start_date"`
	EndDate   pgtype.Timestamptz `json:"end_date"`
}

func (q *Queries) CreateTournament(ctx context.Context, arg CreateTournamentParams) (Tournament, error) {
	row := q.db.QueryRow(ctx, createTournament, arg.Name, arg.StartDate, arg.EndDate)
	var i Tournament
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
	)
	return i, err
}

const deleteMatchTournamentsByMatch = `-- name: DeleteMatchTournamentsByMatch :exec
DELETE FROM match_tournament
WHERE match_id = $1
`

func (q *Queries) DeleteMatchTournamentsByMatch(ctx context.Context, matchID int32) error {
	_, err := q.db.Exec(ctx, deleteMatchTournamentsByMatch, matchID)
	return err
}

const deleteTournament = `-- name: DeleteTournament :one
DELETE FROM tournaments
WHERE id = $1
RETURNING id, name, start_date, end_date
`

func (q *Queries) DeleteTournament(ctx context.Context, id int32) (Tournament, error) {
	row := q.db.QueryRow(ctx, deleteTournament, id)
	var i Tournament
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
	)
	return i, err
}

const getTournament = `-- name: GetTournament :many
SELECT
    t.id AS tournament_id,
    t.name AS tournament_name,
    t.start_date,
    t.end_date,
    tpm.player_id AS player_id
FROM tournaments t
LEFT JOIN tournament_player_membership tpm ON tpm.tournament_id = t.id
WHERE t.id = $1
`

type GetTournamentRow struct {
	TournamentID   int32              `json:"tournament_id"`
	TournamentName string             `json:"tournament_name"`
	StartDate      pgtype.Timestamptz `json:"start_date"`
	EndDate        pgtype.Timestamptz `json:"end_date"`
	PlayerID       pgtype.Int4        `json:"player_id"`
}

func (q *Queries) GetTournament(ctx context.Context, id int32) ([]GetTournamentRow, error) {
	rows, err := q.db.Query(ctx, getTournament, id)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetTournamentRow{}
	for rows.Next() {
		var i GetTournamentRow
		if err := rows.Scan(
			&i.TournamentID,
			&i.TournamentName,
			&i.StartDate,
			&i.EndDate,
			&i.PlayerID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getTournamentMatchDateRange = `-- name: GetTournamentMatchDateRange :one
SELECT
    MIN(m.date)::timestamptz AS min_date,
    MAX(m.date)::timestamptz AS max_date
FROM match_tournament mt
JOIN matches m ON m.id = mt.match_id
WHERE mt.tournament_id = $1
HAVING COUNT(*) > 0
`

type GetTournamentMatchDateRangeRow struct {
	MinDate time.Time `json:"min_date"`
	MaxDate time.Time `json:"max_date"`
}

// HAVING guards the aggregate: with no matches it returns zero rows (ErrNoRows)
// instead of a (NULL, NULL) row that can't scan into the non-nullable time.Time.
func (q *Queries) GetTournamentMatchDateRange(ctx context.Context, tournamentID int32) (GetTournamentMatchDateRangeRow, error) {
	row := q.db.QueryRow(ctx, getTournamentMatchDateRange, tournamentID)
	var i GetTournamentMatchDateRangeRow
	err := row.Scan(&i.MinDate, &i.MaxDate)
	return i, err
}

const getTournamentStats = `-- name: GetTournamentStats :many
WITH ranked AS (
    SELECT
        ms.player_id,
        ms.match_id,
        RANK() OVER (PARTITION BY ms.match_id ORDER BY ms.score DESC) AS place
    FROM match_tournament mt
    JOIN match_scores ms ON ms.match_id = mt.match_id
    WHERE mt.tournament_id = $1
), agg AS (
    SELECT
        player_id,
        COUNT(DISTINCT match_id)::int                   AS matches_count,
        COUNT(*) FILTER (WHERE place = 1)::int          AS first_count,
        COUNT(*) FILTER (WHERE place = 2)::int          AS second_count,
        COUNT(*) FILTER (WHERE place = 3)::int          AS third_count,
        COUNT(*) FILTER (WHERE place = 4)::int          AS fourth_count
    FROM ranked
    GROUP BY player_id
)
SELECT
    tpm.player_id,
    p.name AS player_name,
    COALESCE(agg.matches_count, 0)::int AS matches_count,
    COALESCE(agg.first_count, 0)::int   AS first_count,
    COALESCE(agg.second_count, 0)::int  AS second_count,
    COALESCE(agg.third_count, 0)::int   AS third_count,
    COALESCE(agg.fourth_count, 0)::int  AS fourth_count
FROM tournament_player_membership tpm
JOIN players p ON p.id = tpm.player_id
LEFT JOIN agg ON agg.player_id = tpm.player_id
WHERE tpm.tournament_id = $1
ORDER BY first_count DESC, second_count DESC, third_count DESC, fourth_count DESC,
         matches_count DESC, p.name ASC
`

type GetTournamentStatsRow struct {
	PlayerID     int32  `json:"player_id"`
	PlayerName   string `json:"player_name"`
	MatchesCount int32  `json:"matches_count"`
	FirstCount   int32  `json:"first_count"`
	SecondCount  int32  `json:"second_count"`
	ThirdCount   int32  `json:"third_count"`
	FourthCount  int32  `json:"fourth_count"`
}

func (q *Queries) GetTournamentStats(ctx context.Context, tournamentID int32) ([]GetTournamentStatsRow, error) {
	rows, err := q.db.Query(ctx, getTournamentStats, tournamentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetTournamentStatsRow{}
	for rows.Next() {
		var i GetTournamentStatsRow
		if err := rows.Scan(
			&i.PlayerID,
			&i.PlayerName,
			&i.MatchesCount,
			&i.FirstCount,
			&i.SecondCount,
			&i.ThirdCount,
			&i.FourthCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listActiveTournamentsForPlayers = `-- name: ListActiveTournamentsForPlayers :many
SELECT t.id
FROM tournaments t
WHERE t.start_date <= $1::timestamptz
  AND $1::timestamptz <= t.end_date
  AND NOT EXISTS (
      SELECT 1 FROM unnest($2::int4[]) AS pid
      WHERE NOT EXISTS (
          SELECT 1 FROM tournament_player_membership tpm
          WHERE tpm.tournament_id = t.id AND tpm.player_id = pid
      )
  )
ORDER BY t.id
`

type ListActiveTournamentsForPlayersParams struct {
	At        time.Time `json:"at"`
	PlayerIds []int32   `json:"player_ids"`
}

// Tournament IDs active at @at whose membership includes EVERY player in @player_ids.
func (q *Queries) ListActiveTournamentsForPlayers(ctx context.Context, arg ListActiveTournamentsForPlayersParams) ([]int32, error) {
	rows, err := q.db.Query(ctx, listActiveTournamentsForPlayers, arg.At, arg.PlayerIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []int32{}
	for rows.Next() {
		var id int32
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTournaments = `-- name: ListTournaments :many
SELECT
    t.id AS tournament_id,
    t.name AS tournament_name,
    t.start_date,
    t.end_date,
    tpm.player_id AS player_id
FROM tournaments t
LEFT JOIN tournament_player_membership tpm ON tpm.tournament_id = t.id
ORDER BY t.start_date DESC, t.id DESC
`

type ListTournamentsRow struct {
	TournamentID   int32              `json:"tournament_id"`
	TournamentName string             `json:"tournament_name"`
	StartDate      pgtype.Timestamptz `json:"start_date"`
	EndDate        pgtype.Timestamptz `json:"end_date"`
	PlayerID       pgtype.Int4        `json:"player_id"`
}

func (q *Queries) ListTournaments(ctx context.Context) ([]ListTournamentsRow, error) {
	rows, err := q.db.Query(ctx, listTournaments)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTournamentsRow{}
	for rows.Next() {
		var i ListTournamentsRow
		if err := rows.Scan(
			&i.TournamentID,
			&i.TournamentName,
			&i.StartDate,
			&i.EndDate,
			&i.PlayerID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTournamentsByMatchIDs = `-- name: ListTournamentsByMatchIDs :many
SELECT
    mt.match_id,
    t.id AS tournament_id,
    t.name AS tournament_name
FROM match_tournament mt
JOIN tournaments t ON t.id = mt.tournament_id
WHERE mt.match_id = ANY($1::int4[])
ORDER BY t.name
`

type ListTournamentsByMatchIDsRow struct {
	MatchID        int32  `json:"match_id"`
	TournamentID   int32  `json:"tournament_id"`
	TournamentName string `json:"tournament_name"`
}

func (q *Queries) ListTournamentsByMatchIDs(ctx context.Context, matchIds []int32) ([]ListTournamentsByMatchIDsRow, error) {
	rows, err := q.db.Query(ctx, listTournamentsByMatchIDs, matchIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTournamentsByMatchIDsRow{}
	for rows.Next() {
		var i ListTournamentsByMatchIDsRow
		if err := rows.Scan(&i.MatchID, &i.TournamentID, &i.TournamentName); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const playerHasMatchInTournament = `-- name: PlayerHasMatchInTournament :one
SELECT EXISTS (
    SELECT 1
    FROM match_tournament mt
    JOIN match_scores ms ON ms.match_id = mt.match_id
    WHERE mt.tournament_id = $1 AND ms.player_id = $2
) AS has_match
`

type PlayerHasMatchInTournamentParams struct {
	TournamentID int32 `json:"tournament_id"`
	PlayerID     int32 `json:"player_id"`
}

func (q *Queries) PlayerHasMatchInTournament(ctx context.Context, arg PlayerHasMatchInTournamentParams) (bool, error) {
	row := q.db.QueryRow(ctx, playerHasMatchInTournament, arg.TournamentID, arg.PlayerID)
	var has_match bool
	err := row.Scan(&has_match)
	return has_match, err
}

const removeTournamentMember = `-- name: RemoveTournamentMember :exec
DELETE FROM tournament_player_membership
WHERE tournament_id = $1 AND player_id = $2
`

type RemoveTournamentMemberParams struct {
	TournamentID int32 `json:"tournament_id"`
	PlayerID     int32 `json:"player_id"`
}

func (q *Queries) RemoveTournamentMember(ctx context.Context, arg RemoveTournamentMemberParams) error {
	_, err := q.db.Exec(ctx, removeTournamentMember, arg.TournamentID, arg.PlayerID)
	return err
}

const updateTournament = `-- name: UpdateTournament :one
UPDATE tournaments
SET name = $2, start_date = $3, end_date = $4
WHERE id = $1
RETURNING id, name, start_date, end_date
`

type UpdateTournamentParams struct {
	ID        int32              `json:"id"`
	Name      string             `json:"name"`
	StartDate pgtype.Timestamptz `json:"start_date"`
	EndDate   pgtype.Timestamptz `json:"end_date"`
}

func (q *Queries) UpdateTournament(ctx context.Context, arg UpdateTournamentParams) (Tournament, error) {
	row := q.db.QueryRow(ctx, updateTournament,
		arg.ID,
		arg.Name,
		arg.StartDate,
		arg.EndDate,
	)
	var i Tournament
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
	)
	return i, err
}
